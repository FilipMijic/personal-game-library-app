import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'personal-game-library-app',
  webDir: 'www'
};

export default config;
